# UQ Course RAG — serve 运行时最小依赖(docker 镜像用)。
# 仅 /api 在线服务需要的包;爬取/评测/reranker 的重依赖见 requirements.txt。
# 版本与 requirements.txt 对齐,改一处两处都要改。
fastapi==0.136.3
uvicorn==0.49.0
psycopg[binary]==3.3.4
pydantic==2.13.4
requests==2.33.1
