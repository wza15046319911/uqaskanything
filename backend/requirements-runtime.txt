# UQ Course RAG — minimal serve-time runtime dependencies (for the docker image).
# Only the packages the online /api service needs; for the heavy crawling/eval/reranker
# dependencies see requirements.txt.
# Versions are kept in sync with requirements.txt; change one place and you must change both.
fastapi==0.136.3
uvicorn==0.49.0
psycopg[binary]==3.3.4
pydantic==2.13.4
requests==2.33.1
# Bedrock gpt-oss LLM backend (boto3) + AgentCore Runtime entrypoint (bedrock-agentcore)
boto3==1.43.31
bedrock-agentcore==1.14.1
