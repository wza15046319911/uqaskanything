# Optional dependency: cross-encoder reranking eval (rerank_probe.py / kb_eval --rerank).
# Large in size (torch is hundreds of MB), not part of the base install; install on demand only when doing rerank research/eval:
#   pip install -r requirements-rerank.txt
# Conclusion in docs/rerank_answerability_findings.md: the reranker fixes recall, not refusal, so it is off by default in production.
torch>=2.0
sentence-transformers>=3.0
