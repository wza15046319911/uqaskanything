# UQ Course RAG — backend runtime dependencies
# Install: python3 -m pip install -r requirements.txt
fastapi==0.136.3
uvicorn==0.49.0
psycopg[binary]==3.3.4
pydantic==2.13.4
requests==2.33.1
# Bedrock gpt-oss LLM backend (boto3) + AgentCore Runtime entrypoint (bedrock-agentcore)
boto3==1.43.31
bedrock-agentcore==1.14.1
beautifulsoup4==4.14.3
trafilatura==2.1.0
lxml==6.1.1

# For knowledge base (KB) crawling/eval only (heavier, install on demand)
selenium==4.44.0
undetected-chromedriver==3.5.5
# uc 3.5.5 imports distutils; Python 3.13 removed it, so setuptools<81 is needed to provide a compatibility shim
setuptools<81
playwright==1.60.0
sentence-transformers==5.5.1
# KB vocabulary (answerability gate) Chinese word segmentation; pure CPU, used only by build_kb_vocab for offline table building
jieba==0.42.1
