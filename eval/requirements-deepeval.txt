# Dependencies for DeepEval eval only. Must use a separate venv: deepeval needs openai>=2,
# which cannot coexist in the same environment with the ragas set (langchain-openai requires openai<2).
# Install: python3 -m venv eval/.venv-deepeval
#       eval/.venv-deepeval/bin/pip install -r eval/requirements-deepeval.txt
#       cp eval/.env.example eval/.env   # shares the same .env as ragas (fill in DEEPSEEK_API_KEY)
# judge LLM goes through DeepSeek (deepeval 4.x has a built-in DeepSeekModel); the metrics are all LLM-as-judge, no embedding.
# The pinned versions have not been verified online on this machine; if install fails, adjust based on the deepeval 4.0.x docs.
deepeval==4.0.6
python-dotenv==1.2.2
requests==2.34.2
