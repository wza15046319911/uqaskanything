# Dependencies for RAGAS eval only (decoupled from the backend runtime, installed in a separate virtualenv in this directory)
# Install: python3 -m pip install -r eval/requirements.txt
# judge LLM goes through DeepSeek (OpenAI compatible, via langchain-openai), embedding goes through local Ollama bge-m3.
ragas==0.2.14
langchain-openai==0.2.14
langchain-ollama==0.2.1
datasets==3.2.0
requests==2.33.1
python-dotenv==1.0.1
