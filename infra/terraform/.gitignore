.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
uq_courses.dump
# Lambda build artifacts (boto3 is pip-installed into build/ then zipped)
lambda/build/
lambda/proxy.zip
lambda/breaker.zip
# keep .terraform.lock.hcl (commit it to lock provider versions)
